build:
  environment:
    php:
      version: "7.2"
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
    excluded_paths: [tests/*]

tools:
    external_code_coverage:
      runs: 6
